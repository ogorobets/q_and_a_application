'use strict';

require('cryptojs');

module.exports = function GravatarFactory() {
    var avatarSizePx = '41';
    var baseUrl = 'http://www.gravatar.com/avatar/';
    return function (emailAddress) {
        return baseUrl + Crypto.MD5(emailAddress) + '?size=' + avatarSizePx;
    };
};