'use strict';

var gravatar = require('./gravatar-service'),
    email = require('./email-service'),
    search = require('./search-service'),
    socket = require('./socket-service'),
    ioOperations = require('./io-operations-service');

module.exports = angular.module('app.services', [])
    .factory('Gravatar', gravatar)
    .factory('Email', email)
    .factory('Search', search)
    .factory('Socket', socket)
    .factory('IoOperations', ioOperations);